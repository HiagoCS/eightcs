{
  "name": "eightcs_profile",
  "version": "1.0.0",
  "description": "run frontend and backend",
  "homepage": "https://github.com/HiagoCS/eightcs#readme",
  "bugs": {
    "url": "https://github.com/HiagoCS/eightcs/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/HiagoCS/eightcs.git"
  },
  "license": "ISC",
  "author": "HiagoCS",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "db:init": "npm --prefix backend run db:reset && npm --prefix backend run db:generate && npm --prefix backend run db:migrate && npm --prefix backend run db:seed",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "setup": "node setup.js"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "rimraf": "^6.1.3"
  },
  "dependencies": {
    "@fastify/static": "^10.1.4",
    "dep": "^1.5.8"
  }
}
